# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

# meteor libraries
meteor-base@1.0.4              # Packages every Meteor app needs to have
ecmascript@0.6.2-beta.1               # Enable ECMAScript2015+ syntax in app code
es5-shim@4.6.15                # ECMAScript 5 compatibility for older browsers.
check@1.2.4                          # Check whether a value matches a pattern
tracker@1.1.1                  # Meteor's client-side reactive programming library
session@1.1.7                        # Session variable

# standard libraries
jquery@1.11.10                 # Helpful client-side library
less@2.7.9-beta.1                           # Leaner CSS language

# ui
static-html                    # Define static page content in .html files

# data
mongo@1.1.15-beta.1                   # The database Meteor supports right now
apollo                         # 🚀 Add Apollo to your Meteor app

# accounts
accounts-password@1.3.3              # Password support for accounts

# mobile
crosswalk@1.7.0                      # Makes your Cordova application use the Crosswalk WebView instead of the System WebView on Android
mobile-experience@1.0.4        # Packages for a great mobile UX

# production
standard-minifier-css@1.3.3-beta.1    # CSS minifier run for production mode
standard-minifier-js@1.2.2-beta.1     # JS minifier run for production mode
ddp-rate-limiter@1.0.6               # The DDPRateLimiter allows users to add rate limits to DDP methods and subscriptions.

# testing
shell-server@0.2.1             # Server-side component of the `meteor shell` command

# security
audit-argument-checks@1.0.7          # Try to detect inadequate input sanitization
